# Python 3.12. Direct dependencies of src/, pinned to the versions the
# pipeline was run with, GPU variant: FAISS on CUDA 12 (MIT). XGBoost's wheel
# already includes CUDA support. Used by `make init GPU=1`.

# data
numpy==2.5.3
pandas==3.0.6
pyarrow==25.0.1

# normalization (Indic script -> ASCII fallback; ISC license)
anyascii==0.3.3

# similarity index + features + model
faiss-gpu-cu12==1.14.1
scikit-learn==1.9.1
scipy==1.18.1
joblib==1.6.0
RapidFuzz==3.14.6
xgboost==3.4.1
psutil==7.2.2

# notebooks (src/notebooks/)
ipykernel==7.3.0
matplotlib==3.11.2
